{
  "name": "backend-challenge_cbernardes",
  "version": "0.0.0",
  "description": "TypeScript + Mongoose + MongoDB + Express API Server",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "npm run build && node build/index.js",
    "build": "tsc",
    "test": "npm run build && jest --forceExit --detectOpenHandles"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
